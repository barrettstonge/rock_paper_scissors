# rock_paper_scissors
Odin Project: create a game of rock, paper, scissors. Played entirely on consoler. No GUI.

Pseudocode{
    ask for user input "Woudld you like to play?" 
        if user input is yes begin program
          if user input is no loop back to intial ask
    ask user input "rock, paper, or scissors?"
        and then randomly select either "rock, paper, or scirros" and compare to users input.
                parameters: rock>scissors , paper>rock, scissors>paper
    compare user input !== to radom comp selection
            if comp > user
                message "you lose"
            if user > copmp
                 message "you win!"
}
